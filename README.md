# Intro: Git

このリポジトリは、Gitの基本操作を学ぶための練習用プロジェクトです。

## 内容
- `git init` でリポジトリを作成
- `git add` で変更をステージ
- `git commit` で履歴を記録
- `git push` でGitHubに反映

## 目的
GitとGitHubの使い方を実際に手を動かして体験すること

## 作者
原（Hara Hiroto）
